# 15.7 Polish

Improve command output and user experience with colored tree glyphs, `--json` output on read commands, stdin body support via `--body -`, and clear errors for unknown IDs, ambiguous prefixes, and related edge cases.
