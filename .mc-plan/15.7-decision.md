Phase 15.7 polish kept output behavior CLI-local: read commands now support `--json`, tree formatting uses the existing `[display]` config plus `--no-color`, and `--body -` reads stdin for add/edit without changing the storage model or starting Tailnet/autostart work.
