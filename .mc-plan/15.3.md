# 15.3 Core CRUD

Implement core task CRUD endpoints and matching CLI commands: `add`, flat `list`, `show`, `edit`, and `rm`. Include server-side short-ID prefix resolution so the CLI becomes usable at the end of this phase.
