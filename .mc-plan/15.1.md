# 15.1 Scaffolding

Initialize the Go module and project layout under the repository root. Add the planned `cmd/loose-ends`, internal package, and migration directories; wire baseline dependencies; implement the TOML config loader; and provide the initial CLI skeleton with help plus `config show`, `config set`, and `config path` commands.
