# 15.4 Lifecycle

Add task lifecycle transitions in the API and CLI commands for `done`, `partial`, `archive`, `duplicate`, and `reopen`. Extend listing with `--all` and `--status` filters and plumb `duplicate_of_id` through the model and storage layers.
