# 15.2 Storage + Service Core

Create initial migration files for tasks, tags, and task-tags, then add the storage repository layer backed by pgx. Introduce the HTTP service with `/healthz` and `/version`, plus `loose-ends serve` on loopback and `loose-ends migrate`.
