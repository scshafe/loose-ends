# 15.8 Tailnet + Auto-Start

Add the tsnet listener alongside loopback, document the Tailscale auth-key flow, and provide service startup examples. Include sample launchd and systemd user units, optionally add `loose-ends serve --install-launchd`, and update the README with install, service, and common command guidance.
