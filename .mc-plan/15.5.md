# 15.5 Hierarchy + Ordering

Support parent-child relationships and ordering. Add `parent_id` across the API, implement `POST /tasks/:id/move`, add float `sort_key` insert and move logic with cycle prevention, and expose CLI support for `--parent`, `move`, `--tree`, and `rebalance`.
